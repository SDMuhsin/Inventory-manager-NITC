import { TemplateProperty } from './template-property.model';

describe('TemplateProperty', () => {
  it('should create an instance', () => {
    expect(new TemplateProperty()).toBeTruthy();
  });
});
